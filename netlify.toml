# Build the app in qa-blog (repo root config)
[build]
  base = "qa-blog"
  command = "npm run build"
  publish = "qa-blog/dist"

# SPA fallback: every path serves index.html (React Router handles routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
